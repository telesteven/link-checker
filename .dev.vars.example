# Copy to .dev.vars for local `vite` dev.
# There is no auth bypass — requests must carry valid Cf-Access-* headers,
# verified against these once you configure a Cloudflare Access application
# for this Worker.
ACCESS_TEAM_DOMAIN=https://your-team.cloudflareaccess.com
ACCESS_AUD=your-access-application-aud-tag
